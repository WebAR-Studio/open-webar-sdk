# AR 360 template

Minimal Vite starter for **AR 360** with `@web-ar-studio/webar-engine-sdk`.

## Run

```bash
npm install
npm run dev
```

If needed, copy `.env.example` to `.env` and set `VITE_API_KEY`.
